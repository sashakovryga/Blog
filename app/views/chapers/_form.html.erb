<%= form_for(@chaper) do |f| %>
  <% if @chaper.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chaper.errors.count, "error") %> prohibited this chaper from being saved:</h2>

      <ul>
      <% @chaper.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
